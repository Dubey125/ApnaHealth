# Phase 01 — Database
Read CLAUDE.md and docs/product/DATA_MODEL.md.
Implement the locked Prisma schema and migrations. Use cuid internal IDs and application-generated nanoid public ticket IDs. Add constraints/indexes and a seed clinic, owner, front desk, two doctors, one session and sample tokens.
Do not build UI or business APIs beyond seed support.
Run migration, seed and Prisma inspection. Report actual output.
