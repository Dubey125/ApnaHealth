# Phase 10 — Analytics + research export
Read CLAUDE.md and docs/product/QUEUE_RULES.md.
Implement reports for tokens, wait, consultation duration, prediction error and window hit rate, plus anonymized CSV export. Exclude names, phones, email and medical free text.
Test metrics with synthetic data.
