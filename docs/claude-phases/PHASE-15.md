# Phase 15 — Doctor experience
Read CLAUDE.md and docs/product/DESIGN_SYSTEM.md.
Redesign the doctor dashboard (/app/doctor) and the consultation record workspace (/app/queue/[sessionId]/token/[tokenId]/record), including its patient-history section.
Keep the current-patient numeral prominent (DESIGN_SYSTEM.md's numeric-display type role) — this is used at the point of care for a quick glance, not a dense dashboard.
Do not change the consultation-record field set, consent logic, or access-event logging.
Verify at 768px and 1280px (doctor-facing, desktop/tablet-optimized per DESIGN_SYSTEM.md).
